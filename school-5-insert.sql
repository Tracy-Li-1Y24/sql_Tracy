INSERT INTO students (
    first_name,
    last_name,
    email
) VALUES (
    "Tracy",
    "Li",
    "tracy.li@kellogg.northwestern.edu"
);
-- when creating a table, specify I want a column for primary key
-- but no need to add value to primary key column because sql generates it automatically